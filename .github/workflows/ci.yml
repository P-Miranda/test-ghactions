name: ci

on:
  push:
    branches: [ master ]
  pull_request:
    branches: '*'

jobs:
  ci:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: test
        run: make test
